<!-- Heading -->

<div class="top-header-container">
  <div class="branding">
    <h1>Connected Minds Lambeth</h1>
  </div>
  
  <!-- Login Button -->
  <div class="user-log-in-container">
  <% if (user) { %>
    <a>logged in as <%= user.username %></a>
  
    <a href="<%= baseUrl %>/logout" id="logoutButton" class="user-log-in">Log Out</a>
  <% } else { %>
    <a href="<%= baseUrl %>/login" id="loginButton" class="user-log-in">Log in</a>
  <% } %>

  <a href="<%= baseUrl %>/logout" id="logoutButton" class="user-log-in">Log Out</a>

  </div>
</div>

<!-- Nav bar -->
<nav class="navbar">
  <a href="<%= baseUrl %>/">Home</a>
  <% if (user) { %>
    <% if (user.role === 'supa_admin') { %>
      <a href="<%= baseUrl %>/manage-users/add">Add User</a>
      <% } %>
    <% if (user.role === 'supa_admin') { %>
      <a href="<%= baseUrl %>/manage-activity/add">Add Activity</a>
      <% } %>
  <% } %>
</nav>


<!-- Include the modal partial -->
<%- include("./modal.ejs") %>